language: python
python:
  - "3.5"
install:
-   sudo apt-get install pandoc elasticsearch
-   sleep 5
-   sudo service elasticsearch start
-   sleep 5
-   pip install -r requirements.txt
-   pip install -r requirements-test.txt
script:
-   ./pep8.sh
-   tox
